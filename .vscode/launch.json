{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "server",
            "program": "server/index.js",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/server/.env"
        }
    ]
}
